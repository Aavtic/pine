fn main() {
    let s = "a";
    let i = 0;

    while i < 10 {
        if s == "a" {
            s = "b"
        } else {
            s = "a"
        }
        i = i + 1
    }

    return if s == "a" { 1 } else { 0 }
}
