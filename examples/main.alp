fn add(a: i32, b: i32) -> i32 {
    return a + b
}

fn mul(a: i32, b: i32) -> i32 {
    return a * b
}

fn main(){
    let a = 10
    let b = 20
    let isAdd = 0
    let res = if isAdd == 1 {
        add(a, b)
    } else {
        mul(a, b)
    }

    return res
}
