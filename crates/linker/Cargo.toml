[package]
name = "linker"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { path = "../../crates/utils" }
config = { path = "../../crates/config" }
inkwell = { version = "0.7.0", features = ["llvm21-1"] }
