use io

fn second(other: str) -> i32 {
    let seconds_in_minute = 60
    io.println(other)
    if other == "minute" {
        return seconds_in_minute
    } else if other == "hour" {
        return seconds_in_minute * 60
    } else {
        return 0
    }
}
